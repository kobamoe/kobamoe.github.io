Put your triathlon/race photo here as `hero-triathlon.jpg`.

Recommended: a wide/landscape photo (roughly 4:3), reasonably compressed
(under ~500KB) so the page loads fast. index.html already points to
assets/hero-triathlon.jpg — just drop your file in with that exact name,
or update the src path in index.html to match your filename.
